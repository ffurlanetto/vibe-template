; mypy — static type checking (AGENTS.md B3: no Any in public signatures).
[mypy]
python_version = 3.12
strict = True
warn_unreachable = True
show_error_codes = True
files = src

[mypy-tests.*]
; Tests may be looser than production code, but never untyped.
disallow_untyped_defs = False
